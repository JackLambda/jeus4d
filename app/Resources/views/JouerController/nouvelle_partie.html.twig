{% extends "::base.html.twig" %}

{% block title %}AppBundle:JouerController:nouvellePartie{% endblock %}

{% block body %}

    <div class="container" style="color: black;">
<h1 style="color: #DE6262;">Créez une nouvelle partie</h1>

    {{ form_start(form) }}      {# Ouverture du formulaire, balise form #}
        {{ form_widget(form) }} {# Ecriture des champs #}
        <br>
        <input class="btn btn-login" type="submit" value="Créer partie" /> {# ajout du champ submit #}
    {{ form_end(form) }}        {# fermeture du formulaire #}
        <br>
        <div class="row">
            <div class="col-md-3 col-sm-4" style="margin: 0 auto;">
                <p class=""><a class="btn btn-login" href="{{ path('homepage') }}">Retour au menu</a></p>
            </div>
        </div>

    </div>
{% endblock %}
